#include<stdio.h>
int main() { 
    int hour;
    int minutes;
    printf("Enter the number of minutes:");
    scanf("%d", &minutes);
    hours = minutes/60;
    prinf("&d minutesis equal to %d hours", minutes,hours);

    return 0;
} 
