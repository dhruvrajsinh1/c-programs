#include <stdio.h>

int main() {
int a, b, ans;

printf("Enter value of a: ");
scanf("%d", &a);

printf("Enter value of b: ");
scanf("%d", &b);
ans = a * b;
printf("ans (a * b) = %d\n", ans);
return 0;
}
